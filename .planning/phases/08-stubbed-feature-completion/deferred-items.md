# Phase 08 Deferred Items

## Pre-existing Errors (Not from Plan 08-03)

1. **sync_providers.dart:554** - `missing_required_argument` for `activityLocalDataSource` and `pipelineLocalDataSource` in SyncNotifier constructor. This is from unstaged Phase 07 or Phase 08-01/04 changes that are incomplete.

2. **app_router.dart:687** - `undefined_named_parameter` for `entityId` on SyncQueueScreen. From unstaged Phase 07 work.
